Testing and Support
	•	Developed scripts to extract unique error reasons at the agreement level from large sets of failed records, enabling faster and more accurate issue analysis.
	•	Assisted team members in production release preparation, ensuring smooth deployment processes.
	•	Supported DBI Journal Posting analysis and testing from the House development perspective.
	•	Provided support during COTC tranche migrations, assisting with deployments and resolving issues when they arose.
	•	Collaborated with the testing team (Sonali, Neelam, Ravneet) during EUREX, LCHSA, and LCHFX migrations, performing end-to-end testing alongside them.
	•	Offered daily support to the testing team during UAT, troubleshooting technical challenges and ensuring uninterrupted testing.
Control-M Deployment
	•	Created a separate sub-application in the monitoring section and moved all COTC Control-M jobs under DBMRGN for better organization and monitoring.
	•	Enabled Control-M alerts for all COTC jobs, identified issues, and added alerts to House and COTC related jobs to capture failures proactively.
	•	Renamed existing Control-M jobs to align with the new sequencing for EUREX, LCHSA, and LCHFX.
	•	Analyzed and adjusted FileWatcher and import job timings, and added necessary dependencies to ensure proper workflow execution.
	•	Created separate Cutoff jobs for each CCP, including EUREX Cutoff Job 27, and ensured the UK Cutoff job runs before feed load to prevent recurrence of previous scheduling issues.
	•	Created a new F3Job for Clearing Task UK to support migration and operational workflows.
COTC Migration (Tranche wise)
	•	Added FileWatcher and feed import Control-M jobs for each CCP: LCH SA, LCH FX, and Eurex.
	•	Inserted SQL queries into the F3Job table to create Dbmargin jobs for importing feed files.
	•	Supported Migration Readiness from the CMV perspective, ensuring smooth migration of agreements across all tranches.
	•	Created External Exposures Reset 3 workflow jobs for clearingtaskUS, covering BlackRock US timing.
	•	Updated Client CCPs Migration Readiness by making necessary changes in F3Jobs.
	•	Prepared for Tranche 8 bulk migration of LCH SCM, SA, FX & Eurex agreements by creating F3Jobs for planned agreements.
	•	Deployed all scripts required to update agreements during each migration for CCPs: LCH, Eurex, LCHSA, LCHFX.
	•	Added dependencies on Cutoff jobs, DER jobs, and Cashflow Autobooking jobs to maintain correct workflow execution.
	•	Adjusted timing of FileWatcher and import jobs for each CCP to prevent overlaps and ensure smooth processing.
	•	Coordinated with L2 team to add Geneous alerts on incoming files from DBCS and documented the process on Confluence.


MCUBE Balance Report Development
	•	Developed stored procedure logic in the dbmarginil schema to load daily price and haircut data from dbmargin tables.
	•	Created Control-M jobs and scripts to automate the process, scheduled Sunday–Thursday at 9:30 PM EST, aligned with DBREX workflows.
	•	Implemented the MCUBE Balance Report generation procedure, resolving multiple data outliers through detailed testing and validation.
	•	Built a Java utility to identify discrepancies between DBMargin and DBREX MCUBE Balance reports, improving reconciliation accuracy.
	•	Collaborated with Rajesh to understand DBREX report logic (developed in Informatica and PL/SQL) and align both reporting flows.
	•	Conducted multiple production parallel runs to fix performance issues and validate results.
	•	Provided knowledge transfer (KT) to the MCUBE team, incorporated their feedback, and achieved final sign-off after stable production validation.
	•	Optimized performance and archival process by aggregating 60-day-old balances into local tables, archiving older data into history tables, and implementing table partitioning — reducing report generation time to under 12 minutes and ensuring smooth archival of 65-day-old data.
	•	Updated the feed to convert the As_of_date to yyyy-MM-dd format as per MCUBE team requirements.


CASS Report Development for OPS and Client Service Teams
	•	Documented detailed business and technical requirements on the Confluence page.
	•	Designed and developed a new template for storing daily exposure data, including all required reporting columns.
	•	Created F3 jobs and Control-M jobs to automate daily report generation.
	•	Developed DSS flow service configuration to load the report data daily into the staging table of the dbmarginil schema.
	•	Designed and created the staging and archival tables for data management.
	•	Implemented stored procedure logic to generate the CASS report, along with scripts to call the procedure and generate separate reports for both OPS and Client Service teams.
	•	Enhanced the logic later to include additional columns (e.g., CCP, FX_RATE) missing from the original DER template.
	•	Modified the DBMargin template, staging table, and archival table to accommodate these changes.
	•	Updated DSS configurations, reloaded data, and performed comprehensive archival testing.
	•	Implemented Repgen and Control-M changes to ensure smooth report generation and scheduling.
	•	Conducted a detailed handover session and explained all changes to Rajesh.
MCUBE Balance Report Development
	•	Developed stored procedure logic in the dbmarginil schema to load daily price and haircut information from dbmargin tables.
	•	Created Control-M jobs and supporting scripts to automate the data load process, scheduled Sunday–Thursday at 9:30 PM EST, aligned with the DBREX job schedule.
	•	Designed and implemented a stored procedure to generate the MCUBE Balance Report, resolving multiple data outliers through extensive testing and validation.
	•	Developed a Java utility to identify and analyze outliers between the DBMargin and DBREX MCUBE Balance reports.
	•	Collaborated closely with Rajesh to understand the DBREX MCUBE Balance report generation logic (built in Informatica and PL/SQL).
	•	Conducted multiple parallel production tests and deployments to address performance issues and data mismatches.
	•	Provided knowledge transfer (KT) to the MCUBE team, addressed their feedback and enhancement requests, and obtained final sign-off after successful production validation.
	•	Implemented a feed enhancement to convert As_of_date into yyyy-MM-dd format as per MCUBE team requirements.
	•	Achieved significant performance optimization by:
	•	Aggregating 60-day-old balances into local tables for faster data retrieval.
	•	Archiving 2-month-old price and haircut data into history tables to improve performance.
	•	Adding table partitioning, reducing report generation time to under 12 minutes.
	•	Automating archival of data older than 65 business days via the dbmarginil archival process.

House Development
	•	Built logic in cs-post-ingestion-service to handle GET HTTP requests with query parameters, enabling automated triggering of workflows.
	•	Completed deployment tasks including creation of tables, views, types, sequences, packages, and procedures, with all necessary permissions granted for House development.
	•	Created jobs for importing House Offset and ABF data into dbmargin.
	•	Developed new jobs and scripts to call dbmarginil procedures for updating DAILY_COB_DATE tables.
	•	Set up production directories for SANTANDER, SWAPAGENT, and Archive, and applied proper permissions.
	•	Renamed Asset Booking table from STAGING_COTC_ASSET_BOOKING_DBCS to STAGING_COTC_ASSET_BOOKING_HOUSE_DBCS, including creation of the corresponding history table, and updated the ABF House package to reflect the new table name.
	•	Implemented House CCPs migration readiness changes, including removal of user confirmations from all jobs, along with package and script updates to support report generation.
	•	Executed production deployment for new Control-M jobs for House ABF and Offset report generation, including deployment of all supporting scripts.
	•	Added all necessary job dependencies to ensure proper execution order for House workflows.
	•	Created all House directories and applied 775 permissions to directories and subdirectories for proper access control.
	•	Created staging tables for loading AHV, SSI, and Asset Booking DBS data in the dbmarginil schema, granting access to DBM_COTC_USER.
